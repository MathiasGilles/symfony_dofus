{% extends 'base.html.twig' %}
{% block title %}Element
{% endblock %}
{% block body %}
	<div>
		<div class="col-lg-12">
			<div class="card shadow mb-4">
				<div class="card-header py-3 d-flex justify-content-between">
					<h6 class="m-0 font-weight-bold ">Element</h6>
					<a href="{{path('element_new')}}" class="d-block btn btn-sm btn-success">
						+ Create element
					</a>
				</div>
				<div class="card-body">
					<div class="table-responsive">
						<table id="example" class="display" style="width:100%">
							<thead>
								<tr>
									<th>Name</th>
								</tr>
							</thead>
							<tbody>

								{% for element in element %}
									<tr>
										<th>{{ element.name }}</th>

										<td>
											<a class="d-inline-block" href="{{ path('element_edit', { id: element.id }) }}">
												<i class="text-warning fas fa-edit fa-fw"></i>
											</a>

											<a class="d-inline-block" href="{{path('element_delete', { id: element.id }) }}">
												<i class="text-danger fas fa-trash-alt fa-fw"></i>
											</a>
										</td>
									</tr>
								</td>
							</tbody>
						</tr>
					{% endfor %}
				</tbody>
				<tfoot>
					<tr>
						<th>Name</th>
					</tr>
				</tfoot>
			</table>
		</div>
	</div>
</div></div></div>{% endblock %}
