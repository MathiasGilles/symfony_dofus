{% extends "base.html.twig" %}

{% block body %}
    <h1>Sign In</h1>

    {{form_start(form)}}

    {{form_widget(form)}}

    <button type="submit" class="btn btn-success">Submit</button>
    {{form_end(form)}}
{% endblock %}